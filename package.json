{
  "name": "basecamp-cleaning-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "seed:build": "node scripts/generate-seed.mjs",
    "dev": "wrangler pages dev public --d1 DB=basecamp-cleaning --local",
    "db:init:local": "wrangler d1 execute basecamp-cleaning --local --file=schema.sql && wrangler d1 execute basecamp-cleaning --local --file=seed.sql",
    "db:init": "wrangler d1 execute basecamp-cleaning --remote --file=schema.sql && wrangler d1 execute basecamp-cleaning --remote --file=seed.sql",
    "db:seed": "wrangler d1 execute basecamp-cleaning --remote --file=seed.sql",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
